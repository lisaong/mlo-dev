{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build bmm",
            "type": "shell",
            "command": "nvcc -g -G -o bmm bmm.cu",
            "problemMatcher": [
                "$nvcc"
            ],
            "options": {
                "cwd": "${workspaceFolder}/banded_matmul"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}